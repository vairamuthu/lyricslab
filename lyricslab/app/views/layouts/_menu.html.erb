 <nav id="mainMenu" class="clearfix">
                          
                            <ul> <% if user_signed_in?%>
                            	 <% if !current_user.is_admin %>
                                 <%= render :partial =>'layouts/main_menu'%>
                                 <% current_user.user_menus.each do |menu|%>
                                 <%# raise menu.inspect%>
                                  <li><a href="<%= menu.menu.url%>" class="firstLevel <%= current_user.user_menus.first == menu ? "active" : ""%>"><%= role_menu.menu.name%></a>
                                            </li>
                                		
                                 <% end %>                
                                <% elsif current_user.is_admin %>
                              
                                		<% admin_menus.each do |role_menu| %>
                                			<li><a href="<%= role_menu.menu.url%>" class="firstLevel <%= admin_menus.first == role_menu ? "active" : ""%>"><%= role_menu.menu.name%></a>
                                            </li>
                                		<% end %>	
                                <% end %>	
                                <% end %>
                                <li>
                                	<% if !user_signed_in?%> 
                                	<%= render :partial =>'layouts/main_menu'%>
                                	<li>                               	
                                	<%= link_to "Sign In", new_user_session_path%>
                                	</li>
                                	<%#= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %>
	                   				<% else%>
                                    <li>
                                    <%=link_to("Sign Out", destroy_user_session_path, :method => :delete) %>               
                                </li>
                                    <% end %></li>
                             
                               
                                    
                                
                            </ul>
                        </nav>
